---
const { height = 24, width = 24, color = '#c9c9c9', hidden = false } = Astro.props;
---

<svg class={`${hidden ? 'hidden' : ''}`} viewBox="0 0 24 24" height={height} width={width} fill={color}>
  <path
    fill-rule="evenodd"
    d="M23,20 L23,6 L12,15 L1,6 L1,20 L23,20 Z M12,12 L22,4 L2,4 L12,12 Z"
  />
</svg>
